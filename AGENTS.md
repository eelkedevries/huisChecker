# AGENTS.md

This is the shared root instruction file and source of truth for repository-level agent rules.

After reading this file:
- read 
- read only the task-specific docs explicitly named in the prompt
- use  only if it is available locally and explicitly relevant

## prompt file location and resolution

Implementation prompts for planned multi-step work live in:
- 

When you are told to run a numbered prompt, resolve it to the matching file in .
Examples:
- prompt 1 -> 
- prompt 2 -> 

If the prompt request includes both a number and a filename, follow the explicit filename.
If it includes only the number, use the matching numbered file.
